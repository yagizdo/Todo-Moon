import 'package:flutter/material.dart';

class StatisticComp extends StatelessWidget {
  const StatisticComp({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
